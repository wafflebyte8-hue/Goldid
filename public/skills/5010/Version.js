"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "GolDid Memory And Sessions Fixer",
  "Description": "Debug GolDid memory files, saved sessions, context loading, and stale state behavior.",
  "Usage": "Use when a user reports memory, sessions, saved context, resume behavior, or project instruction files behaving incorrectly in GolDid.",
  "Model_tested": ["gpt-5","claude-sonnet"]
};
