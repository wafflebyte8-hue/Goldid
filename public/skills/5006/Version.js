"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "GolDid Release Captain",
  "Description": "Perform GolDid version bumps, release notes, package metadata, docs, and visible UI version updates.",
  "Usage": "Use when a user asks to bump GolDid version numbers, prepare a release, update release documentation, or align visible version text.",
  "Model_tested": ["gpt-5","claude-sonnet"]
};
