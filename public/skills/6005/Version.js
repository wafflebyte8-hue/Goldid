"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "API Designer",
  "Description": "Design, review, or improve APIs for any backend, library, service, or integration.",
  "Usage": "Use when a user asks for endpoint design, request/response schemas, SDK shape, backward compatibility, or API review.",
  "Model_tested": ["gpt-5", "claude-sonnet"]
};
