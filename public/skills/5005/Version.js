"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "GolDid Skill Publisher",
  "Description": "Publish GolDid skills with registry entries, detail pages, install commands, and metadata files.",
  "Usage": "Use when a user wants to add, publish, update, validate, or list skills in the GolDid public skill marketplace.",
  "Model_tested": ["gpt-5","claude-sonnet"]
};
