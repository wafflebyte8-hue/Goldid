"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "Performance Profiler",
  "Description": "Find and fix performance bottlenecks in any application with measurement-first changes.",
  "Usage": "Use when a user reports slow startup, lag, high memory, slow queries, expensive builds, or performance regressions.",
  "Model_tested": ["gpt-5", "claude-sonnet"]
};
