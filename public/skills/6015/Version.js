"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "Migration Planner",
  "Description": "Plan migrations between frameworks, libraries, providers, data formats, or architectures for any project.",
  "Usage": "Use when a user wants a migration plan, phased rollout, compatibility strategy, or risk assessment.",
  "Model_tested": ["gpt-5", "claude-sonnet"]
};
