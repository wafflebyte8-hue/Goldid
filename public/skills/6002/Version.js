"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "Bug Fixer",
  "Description": "Diagnose and fix bugs in any codebase with narrow patches and targeted verification.",
  "Usage": "Use when a user reports broken behavior, failing tests, crashes, logs, exceptions, or incorrect output in any project.",
  "Model_tested": ["gpt-5", "claude-sonnet"]
};
