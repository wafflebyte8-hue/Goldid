"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "Dependency Upgrader",
  "Description": "Upgrade dependencies in any project while managing breaking changes and lockfile consistency.",
  "Usage": "Use when a user asks to update packages, fix dependency warnings, refresh lockfiles, or migrate between library versions.",
  "Model_tested": ["gpt-5", "claude-sonnet"]
};
