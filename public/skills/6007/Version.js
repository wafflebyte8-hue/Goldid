"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "Security Auditor",
  "Description": "Audit any codebase or configuration for practical security risks and unsafe defaults.",
  "Usage": "Use when a user asks for a security review of code, scripts, dependencies, infrastructure, config, or release artifacts.",
  "Model_tested": ["gpt-5", "claude-sonnet"]
};
