"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "CI Debugger",
  "Description": "Debug failing CI pipelines, build jobs, tests, and automation in any project.",
  "Usage": "Use when a user provides failing CI logs, build errors, workflow files, or asks why automation is failing.",
  "Model_tested": ["gpt-5", "claude-sonnet"]
};
