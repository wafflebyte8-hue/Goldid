"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "GolDid Packaging Inspector",
  "Description": "Check GolDid npm, Electron, assets, icons, package files, and generated distributables.",
  "Usage": "Use when a user asks about packaging, app icons, npm metadata, Electron build output, package-lock drift, or release artifacts.",
  "Model_tested": ["gpt-5","claude-sonnet"]
};
