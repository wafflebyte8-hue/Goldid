"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "Refactor Planner",
  "Description": "Plan or execute safe refactors in any codebase without changing behavior.",
  "Usage": "Use when a user wants cleanup, decomposition, simplification, architecture improvements, or technical debt reduction.",
  "Model_tested": ["gpt-5", "claude-sonnet"]
};
