"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "GolDid Desktop Launcher Fixer",
  "Description": "Fix GolDid desktop launch failures across Linux, Windows, and macOS packaging paths.",
  "Usage": "Use when a user reports that the GolDid desktop app does not launch, crashes on startup, or opens differently across operating systems.",
  "Model_tested": ["gpt-5","claude-sonnet"]
};
