"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "GolDid Installer Maintainer",
  "Description": "Maintain GolDid setup, uninstall, desktop entries, PATH shims, permissions, and data-retention behavior.",
  "Usage": "Use when a user asks to fix setup.sh, uninstall.sh, install scripts, desktop integration, PATH setup, or cleanup behavior.",
  "Model_tested": ["gpt-5","claude-sonnet"]
};
