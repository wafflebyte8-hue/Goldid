"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "GolDid Tooling Hardener",
  "Description": "Audit and harden GolDid file, shell, web, image, and memory tool behavior.",
  "Usage": "Use when a user asks to change GolDid tools, permissions, sandbox handling, command execution, file writing, web access, or approval behavior.",
  "Model_tested": ["gpt-5","claude-sonnet"]
};
