"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "Data Cleaner",
  "Description": "Clean, validate, transform, or inspect structured data files for any project.",
  "Usage": "Use when a user wants CSV, JSON, YAML, logs, exports, or datasets cleaned, validated, merged, summarized, or converted.",
  "Model_tested": ["gpt-5", "claude-sonnet"]
};
