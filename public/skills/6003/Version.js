"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "Test Builder",
  "Description": "Add focused tests for any project using the existing test framework and local conventions.",
  "Usage": "Use when a user asks to add tests, improve coverage, protect a bug fix, or design regression checks.",
  "Model_tested": ["gpt-5", "claude-sonnet"]
};
