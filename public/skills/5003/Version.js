"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "GolDid CLI Command Builder",
  "Description": "Add or repair GolDid CLI commands with consistent parsing, help text, docs, and desktop parity.",
  "Usage": "Use when a user asks to add, rename, debug, or document a GolDid CLI command or slash command.",
  "Model_tested": ["gpt-5","claude-sonnet"]
};
