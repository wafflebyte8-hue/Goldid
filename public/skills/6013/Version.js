"use strict";

// GolDid normalized skill metadata.
// IMPORTANT: this file is parsed as JSON, so every key MUST be double-quoted,
// with no comments inside the object and no trailing commas.
module.exports = {
  "Author": "Goldid",
  "Name": "Accessibility Checker",
  "Description": "Audit and improve accessibility in any web, desktop, or mobile interface.",
  "Usage": "Use when a user asks for accessibility review, keyboard support, screen reader behavior, color contrast, labels, or semantic markup.",
  "Model_tested": ["gpt-5", "claude-sonnet"]
};
